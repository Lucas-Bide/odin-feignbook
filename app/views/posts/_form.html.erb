<%= form_with model: @post, local: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br />
  <%= f.label :body %>
  <%= f.text_area :body, rows: 10, cols: 60 %><br />
  <%= f.submit %>
<% end %>